This personal project group a set of examples of Design Patterns implementations, such as:

Strategy

To run this project simply run the command associated to the desired pattern

e.g
```
npm run start:strategy
```